# X28 平台内容运营辅助工具 - 第二阶段优化方案

为了解决您运行中发现的体验与抓取痛点，我们将围绕 **UI设计升级、关键词/分类监控、代理自愈及 Nitter 智能分发、自定义 Prompt 模板** 四大方向进行深度重构。

---

## 1. 竞品参考与设计灵感（同赛道领导者）

针对您的软件定位，以下是同赛道中排名前三的领导者平台，我们将参考它们的优秀设计来优化 X28 的后续开发：

1. **Typefully (写作与视觉预览标杆)**
   - **定位**：专注极简、沉浸式的推文与 Thread 写作。
   - **UI特点**：采用极低饱和度的黑灰色背景（Obsidian Black），文字采用纯白与暖灰对比。右侧有极其优雅的“所见即所得”手机/电脑推文排版预览。
   - **借鉴点**：**Obsidian 深灰黑主题配色**，卡片悬浮与点击的微交互动画。

2. **TweetHunter (AI 激发与内容库标杆)**
   - **定位**：AI 驱动的 X 增长与商机获取工具，注重社交聆听（Social Listening）。
   - **功能特点**：支持按关键字、特定类目筛选抓取全网爆火推文，并提供 AI 变体写作（Rewrite）功能。
   - **借鉴点**：**关键词与分类监控列表**，将单账号维度扩展到“话题/关键词”检索维度。

3. **Hypefury (自动化与多维度运营标杆)**
   - **定位**：全能型 X 运营与自动分发引擎。
   - **UI/功能特点**：提供“灵感箱（Inspiration Box）”，分类展示不同细分领域的 Prompt 以及可供套用的模板。
   - **借鉴点**：**预设与自定义 Prompt 库选择器**，允许用户保存和切换不同的写作策略。

---

## 2. 第二阶段拟作出的修改设计

### UI 视觉重构：告别“土气蓝”，走向“高级灰”
- **背景**：采用纯粹的暗夜曜石黑 `#0c0c0e` 代替原来的深蓝色 `#1a1a2e`。
- **面板/侧栏**：使用微亮炭灰 `#16161a`，与背景形成层次感。
- **卡片/输入框**：使用 `#222226`，配合 1px 的精致边框 `#2d2d34`。
- **选中高亮**：使用极具质感的珊瑚红/绯红色 `#e94560` 作为高亮边框和主按钮色。
- **微交互**：为推文卡片添加鼠标悬停（Hover）变色动画（从 `#222226` 变到 `#2b2b30`），大幅度提升操作灵动感。

### 监控机制重构：新增“关键词/话题”监控
- **双模式切换**：左侧账号栏上方新增一个 Tab 切换器（Segmented Button），支持在 **“监控账号”** 和 **“监控关键词”** 之间切换。
- **关键词 RSS 检索**：在关键词模式下，输入关键字，通过 `twiiit.com/search/rss?q={query}` 抓取匹配的最新推文，实现一类话题的自动化追踪。

### 数据抓取与网络自愈（解决“一直显示抓取中”的问题）
1. **自动修复代理变量**：启动时自动检测系统环境变量 `HTTP_PROXY` 和 `HTTPS_PROXY`，如包含括号（如 Windows 常见的 `[http://127.0.0.1]:7897` 异常格式），程序将自动剔除括号进行修复，防止 python 底层网络库崩溃或卡死。
2. **Nitter 智能网关 (twiiit.com)**：不再硬编码不稳定的公共实例，抓取接口统一更换为 `twiiit.com`。这是一个智能分发网关，会自动将请求重定向到当前存活的公共 Nitter 实例上，最大化抓取成功率。
3. **防死锁超时与错误透传**：如果网络超时或抓取彻底失败，界面会立刻终止“抓取中”状态，并在中栏卡片区渲染具体的错误日志（如“代理被拒”或“网络超时”），便于用户排查。

### Prompt 模板管理器（按需上传/选择 Prompt）
- **Prompt 下拉选择**：右侧 AI 面板顶端新增 Prompt 下拉框，支持切换不同运营目标下的 Prompt。
- **默认预设 Prompt**：
  - *标准回复 (Reply)*、*转推评论 (Quote Tweet)*、*系列推文 (Thread)*、*幽默互动*、*专业分析*。
- **模板自由编辑与增删**：
  - 下拉框下方提供大文本框，展示当前选中的 Prompt 模板（支持 `{tweet_content}` 和 `{author}` 标签）。
  - 用户可直接在文本框里微调 Prompt，或点击“保存为新模板”/“删除模板”按钮进行持久化管理（数据存储在 `data/prompts.json`）。

---

## 3. 文件结构变化

```text
e:\Digital Nomads\X28\
├── data/
│   ├── config.json         # 整合存储监控账号与监控关键词列表
│   └── prompts.json        # 持久化存储用户自定义的 Prompt 模板
├── .env                    # 保持原有配置 (增加代理自动清理逻辑)
├── requirements.txt        # 保持原样
└── main.py                 # 深度重构后的主程序
```

---

## 4. 验证计划

1. **代理修复验证**：在环境变量中故意设置损坏的代理 `[http://127.0.0.1]:7897`，启动程序验证是否能够成功联网。
2. **关键词抓取测试**：切换到关键词模式，搜索 "AI agent"，验证是否能通过 `twiiit.com` 获取相关推文。
3. **Prompt 保存测试**：新增一个自定义的 "吐槽风" 模板并保存，重新运行程序检查模板是否成功从 `data/prompts.json` 中读取。
