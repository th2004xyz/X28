# X28 依赖（固定下限版本，提升可复现性，规避已知问题）
customtkinter>=5.2.0,<6.0.0
requests>=2.31.0,<3.0.0
pyperclip>=1.8.2,<2.0.0
python-dotenv>=1.0.1,<2.0.0
pillow>=10.2.0,<12.0.0

# 第三阶段新增（可选，未安装时自动回退）
keyring>=24.0.0,<26.0.0  # 系统凭证存储（API Key 安全保存）
plyer>=2.1.0,<3.0.0      # 系统通知（自动刷新检测到新推文时弹通知）
